import React from "react";

const ContactFormPage = () => {
    return (
        <div>
            All data comes from contact form. Which is submitted by a client.
        </div>
    );
};

export default ContactFormPage;
